{
  "name": "todo-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,green \"npm run dev --prefix todo-backend\" \"npm run dev --prefix todo-frontend\"",
    "install:all": "npm install --prefix todo-backend && npm install --prefix todo-frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}